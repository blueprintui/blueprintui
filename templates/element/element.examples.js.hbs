export const metadata = {
  name: '{{name}}',
  elements: ['bp-{{name}}']
};

export function example() {
  return /* html */`
    <script type="module">
      import '@blueprintui/components/include/{{name}}.js';
    </script>

    <bp-{{name}}></bp-{{name}}>
    `;
}
