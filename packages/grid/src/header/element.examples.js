import { getGrid, grid } from '../examples.data.js';

export const metadata = {
  name: 'header',
  elements: ['bp-grid-header', 'bp-grid', 'bp-grid-column', 'bp-grid-row', 'bp-grid-cell']
};

export function example() {
  return /* html */`
  `;
}
